window.onload = () => {

    // Definitions
    const canvas = document.querySelector('#hello-world-canvas');
    const context = canvas.getContext('2d');

    // Draw line steps
    context.beginPath();
}